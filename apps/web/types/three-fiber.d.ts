/**
 * Type declarations for @react-three/fiber JSX elements.
 *
 * R3F v9 with React 19 uses module augmentation pattern.
 * This extends ThreeElements to include custom Three.js elements if needed.
 *
 * Note: R3F v9 automatically provides JSX types via its own module declaration,
 * but this file ensures compatibility with the TypeScript config.
 */

import '@react-three/fiber';

export {};
