/**
 * Application Configuration Module.
 *
 * Single source of truth for all environment variables used in the app.
 * All exports are client-safe (NEXT_PUBLIC_ prefixed).
 */

export {
  APP_NAME,
  APP_URL,
  APP_KEYWORDS,
  GITHUB_URL,
  STORAGE_PREFIX,
  STORAGE_KEYS,
  THEME_COLOR,
  IS_DEVELOPMENT,
} from './app';
